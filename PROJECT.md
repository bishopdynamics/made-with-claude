# Project: (name not set — see First Run)

(one-line project description — set during first run)

This file is **project-owned**: template migrations never touch it. It holds everything about *this* project that agents must know beyond the shared rules in `AGENTS.md`. Where the two conflict, this file wins.

## Project-specific rules

Adjustments to the shared process for this kind of project: language and toolchain conventions, extra verification steps, paths agents must never touch, anything `AGENTS.md` gets wrong for this project. Fill in during first run and keep current.

-
