# Evergreen Handoff

Durable handoff info — the kind of stuff that should be read at the start of every session, regardless of what the last session did. Keep entries current; delete them when they stop being true.

## Evergreen Entries

- Project: Hephaestion; public site: `whatclaudemade.com`. A polished portfolio/blog showcasing the user's software projects built with Claude and/or Codex under the guidance of BishopDynamics. Homepage title: “What Claude/Codex Made”. Sources of requirements: `docs/idea/initial-idea.md` and the ongoing `docs/idea/design-discussion.md`.
- First project selected (2026-09-07): Half-Life: Continuum Edition, complete and publicly released with some existing media; public repository: <https://github.com/bishopdynamics/Continuum>. Every published project must have at least one representative image. Visual direction: black background, non-white text, muted amber accents, and free fonts.
- The user approved the detailed visual direction on 2026-09-07: near-black/charcoal, warm-gray text, muted amber, Inter and JetBrains Mono, generous spacing, landing tiles, horizontal catalog cards, manually browsed gallery, and dark editor-style code blocks. A single initial project gets a generous homepage tile; development duration uses reviewed Git/release dates. The full `docs/spec/ROOT_SPEC.md` was approved on 2026-09-07; the user subsequently approved the rendered design and accepted Continuum portfolio v1 on 2026-09-08.
- Continuum portfolio v1 is accepted as release-ready: source-backed metadata, the existing full-resolution menu-artwork cover, and the README GIF inline. More gallery images belong to v2. Its prepared `publishedOn` date is 2026-09-08; reconcile first-publication dates at website launch if launch is later. `draft: false` selects an accepted entry for local/public builds but does not deploy the domain.
- BeamVM's approved write-up resumed media/metadata work when the user supplied `docs/SCREENSHOTS.md` and its captures (2026-09-08). Six screenshots are integrated with Rockfield as cover. The user confirmed August 25–September 8, 2026, including initial planning on August 25; display 2 weeks. Languages are Rust/BeamVM DSL/WGSL; tags Games/Graphics/Developer tools. Public source/download URL, attribution, and final assembled-entry review remain pending. Do not expose the login-gated internal remote URL.
- FantasyBoy is in draft review under `docs/spec/FEATURE_SPEC_fantasyboy_onboarding.md`: three existing screenshots, Python/C, Games/Emulation/Developer tools, and approximately February 3–5, 2026 from the user's recollection (2 elapsed calendar days). Public source is `bishopdynamics/FantasyBoy`; no GitHub release was found at intake. Do not replace original dates with much later review/template commits or change the source checkout from this portfolio session.
- Draft previews now show language, tag, and development metadata; missing draft values are labeled pending. This is local authoring behavior only: draft entries and pending labels remain excluded from public output and public search/filter data.
- FantasyBoy is confirmed as the user's **first project with Claude**, while evaluating AI coding capabilities and learning LLM/context/memory workflows. Original work was mainly Sonnet; possible Opus involvement is uncertain. Fable/Astra review and template migration happened much later and must not define the original development endpoint. Describe it as a 6502-inspired fictional console with Game Boy-like inspiration; the real Game Boy did not use a 6502.
- BeamVM's user-confirmed story starts with constrained game-making on a VM/vector CRT, expands through repair challenges and launcher/shell, then Pro hardware with asynchronous drawing to enable a compromised DOOM-derived game. **Only the first two shareware maps were tested; full DOOM/DOOM II conversion is theoretical.** Do not extend tested coverage or borrow Continuum's specific model attribution.
- The user wants GitHub-based hosting and CI publication, with DNS managed in Amazon Route 53 and no self-hosted public infrastructure. This overrides the template's GitLab assumption.
- Rapid local iteration must work without committing or pushing. Pushing to the publishing branch is the final publication step. Pre-commit screening must cover secrets, sensitive files, and personal information.
- Add projects individually with the user: cleanup, screenshot capture, and a blog entry. Prepare a reusable prompt for each project's maintainer LLM to discuss suitable screenshots.
- Initial candidates listed as ready: Half-Life: Continuum edition, Vintage, matrix-screensaver, capture-view. Almost ready: BeamVM, VintageVault. Most target Linux and macOS. Listed readiness does not mean screenshots or writing are already prepared.
- Approved foundation (2026-09-07): static Astro, Markdown content, TypeScript site code, npm with a lockfile, and GitHub Pages via GitHub Actions. Use the standard Astro `src/` layout recorded in `PROJECT.md`.
- GitHub remote: `github`, pointing to `bishopdynamics/made-with-claude`. The user made the repository public on 2026-09-07, resolving the private-repository GitHub Pages plan requirement. Keep `.bishop/` committed per the user's explicit instruction; scan its contents without treating the folder itself as sensitive.
- The repository and project Codex safety-hook definition have recorded trust. The hook matches the trusted template definition, and its guard was verified with synthetic inputs. Do not modify template-owned hook files.
- Development: Node version in `.nvmrc`, npm lockfile, `make setup`, `make run`, `make check`. Git hook lives at `.githooks/pre-commit`; setup installs it. See `docs/development.md` for screening limits and upgrade considerations (TypeScript checker compatibility, foreground Astro API wrapper, direct Secretlint rule registration).
- Slice 1 establishes the project schema/query/search/date contracts and code renderer. Later UI workers should read the API table in `docs/development.md`; use `getPublicProjects()` for public output and explicit development-only draft queries for previews. Taxonomy contains the reviewed identifiers needed by accepted content; add future values during onboarding. Dev/build wrappers explicitly set `NODE_ENV` because this host inherits production; loader watcher roots must remain normalized and listeners registered individually for proper Astro cleanup.
- Slice 2 supplies Page/Seo, header/footer, shared dark styling, real local Inter/JetBrains Mono faces, and prose/code presentation. Page accepts direct props or Markdown frontmatter; `prose` defaults on only for Markdown, and `variant: home` selects the larger introduction. See `docs/development.md` for the complete API and classes; font sources/licenses/checksums live in `public/fonts/README.md`. Preserve the title-bar Copy position so it does not cover code.
- Slice 3 supplies project detail routes/layout/metadata and the `project-gallery` custom element. Gallery props are `{ images, id, coverId?, coverPosition? }`; use a unique ID. Originals load on demand; thumbnails are optimized/cropped. Preserve focus relocation when disabling navigation buttons, modal Tab wrapping, and thumbnail-bound edge detection with matching scroll padding. Full API/verification notes are in the development guide. For persistent native browser preferences/touch tests, the installed mcp-browser Playwright runtime worked where separate connector CDP calls reset state.
- Slice 4 supplies recent homepage tiles, catalog cards, and the `project-index` element using the frozen search/URL helpers. Preserve raw query/caret during typing, safe JSON transport, URL/history state, and static fallback. Dev draft cards are a separate labeled section, never part of public search/counts/options. Fixture builds with symlinked node_modules must use the shared `isolateFixtureCaches(root)` helper to avoid shared Astro/Vite cache contamination. Component APIs and verification notes are in the development guide.
- `archive_1.md` contains handoff history inherited from project-template-a. It describes work on the template and other repositories, not Hephaestion; do not resume its next-session instructions here.
