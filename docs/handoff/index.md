# Handoff Main Index

This is where a new session looks for handoff information from previous sessions. Keep this file minimal: it holds the live current-session block; when it grows too large, split older entries into `archive_<number>.md` next to this file and reference them below. Durable always-read info belongs in `evergreen.md` instead.

## Current

- 2026-09-01 — Reviewed the previous session's uncommitted slice-9 work (an in-tool canary state machine) and then, with the user's agreement, redesigned the whole migration approach instead of finishing it. The stash `slice-9 canary state machine (superseded by ownership redesign)` holds that work. New design, in `docs/spec/FEATURE_SPEC_template_ownership_migration.md`: every copied file has one owner; `PROJECT.md` holds identity and project-specific rules; `AGENTS.md` is template-owned and now carries "Template ownership" and "Task queue" sections; `.gitignore` has a managed block; `dev.sh` sources `dev.local.sh`; `TEMPLATE_VERSION` (now `1`) plus `TEMPLATE_CHANGELOG.md` replace the content-hash marker. `migrate-project-docs.py` was rewritten from 3,800 lines to about 500: deterministic copy/merge, Git-clean gate on managed paths, atomic writes with marker last, and mechanical conversion of old-layout projects that diffs their instructions against the template's Git history and carries custom lines into `PROJECT.md` (`CONVERSION.md` explains the tidy-up). 23 fixture tests pass. A read-only dry run over `/mnt/Fast/projects` classified 59 repositories: 32 convertible, 9 dirty (including the four incident projects), 17 not template. **No real project was changed.** Next: the user runs `./migrate-project-docs.sh /mnt/Fast/projects`, reviews and commits a few converted projects, then tidies carried-over sections per `CONVERSION.md`. The incident recovery for `Mezuril`, `beamvm`, `capsmith`, `capture-view` is still separately pending; they show as `dirty` until then.
- 2026-09-01 — The Codex apply run `20260901-093424-764417` proved direct agent target writes unsafe: protected `.codex` paths caused partial failures, `capture-view` received a false-current marker, and the fixed allowlist rejected legitimate project-specific process documents. Revised the approved migration-dashboard spec so `docs/TASK_QUEUE.md` is the mandatory template gate, agents prepare exact candidates only in isolated run workspaces, the driver owns hook/config/marker changes, apply uses exact backups/restoration, marker invariants are verified, and a canary precedes bulk apply. Added a read-only recovery table: `Mezuril` updated; `beamvm` and `capsmith` are recoverable partials; `capture-view` needs semantic reconciliation; `cleo` received no migration changes. No target cleanup or redesign implementation has been authorized yet.
- 2026-09-01 — Diagnosed the first real parallel Claude inventory run (`20260901-000824-725644`): all 37 analyses exited before execution because Claude Code 2.1.252's variadic `--tools` consumed the positional prompt. Added an explicit `--` prompt boundary to Claude analysis, apply, and worktree-audit commands and made the fake Claude parser reject the broken argv shape. All 37 tests plus Python/shell syntax and diff checks pass. The failed read-only run did not modify target projects and should not be resumed; start a new run. No paid-agent verification was run after the fix.
- 2026-09-01 — Added controlled concurrency for independent read-only migration analyses: automatic affinity-aware jobs use available logical CPUs minus one, `--jobs N` overrides the ceiling, only the main thread persists/renders shared state, interruption terminates active process groups, and unfinished work resumes safely. Worktree handling and apply remain serial. Replaced the temporary `dev.sh --raw` design with a default-yes interactive choice to send the shared `CLAUDE.md` startup prompt; every CLI argument now passes through unchanged. All 36 integration tests plus Python/shell syntax and diff checks pass; no real paid-agent migration was run.
- 2026-08-31 — Made the template shared between Claude Code and Codex: `AGENTS.md` is now canonical, `CLAUDE.md` imports it, orchestration/model/isolation guidance covers both platforms, and the `rm` guard is registered through shared `.agent-hooks/` code for both clients. Added the template-only `migrate-project-docs.sh`, which discovers template-derived Git projects, tracks a content snapshot, and prompts once per nuanced project migration for Claude/Codex/skip while preserving living process data. New projects record their initial snapshot and exclude the migration tool plus personal Claude settings. The launcher now prompts for Claude or Codex and uses Sol for Codex; worker guidance now pins role-appropriate models/reasoning and treats parallel-write worktree isolation explicitly.
- 2026-08-31 — Rebuilt the migration tool as a two-phase Python/terminal-dashboard workflow: one agent selection, immediate discovery/classification feedback, read-only analysis of all projects, a review/exclusion gate, sequential apply, resumable plans/logs, fingerprint-based stale-plan rejection, and post-apply validation. Discovery now stops at top-level Git boundaries and groups linked worktrees; affected projects default to skip, with an optional agent-assisted audit and independently gated, explicitly confirmed, non-forced cleanup that retains branches. Canonical `AGENTS.md` guidance now requires cleanup of completed worker worktrees and documentation of intentional survivors. Twenty-six integration tests pass; a real paid-agent migration was intentionally not run. `dev.sh` gives Sol a 1,000,000-token working context and compacts at 900,000 via per-session overrides.
- 2026-08-05 — Folded Kerno's adoption amendments (from `~/tmp/parallel-notes.txt`) back into the template (commit `2b295bb`): cross-task parallelism exception, explicit `model` on every Agent call, one-worker rule for two-ended protocols, orchestrator-only non-code duties, worktree-cost guidance, worker-runnable vs orchestrator verification split, owned-files on all slices, retrofitting note. Elefant memory captured. Template itself still uninitialized (FIRST_RUN pending) — that's expected; this repo is the template source.

- 2026-09-01 (later) — The user ran the tool and converted the 32 clean projects (uncommitted in each project). Then, with approval, recovered the incident projects: the Codex run's damage was entirely in template-owned paths, so a targeted `git checkout` plus deleting the untracked files it had created restored each to HEAD (backups in this session's scratchpad), preserving the project-owned edits (`Mezuril` initial idea, `capsmith` TODO, `capture-view` bishop tasks). Removed stray uncommitted template `dev.sh` copies from `estro-engine` and `kerno`. The tool then converted `Mezuril`, `beamvm`, `capsmith`, `capture-view`, `estro-engine`, `kerno`, and `sampo` (the user had committed sampo's first-run work meanwhile; its own session then committed the migration and tidied `PROJECT.md`). The old recovery table is now moot. Two tool bugs found and fixed in the process: header blockquote notes were dropped on conversion (patched `PROJECT.md` in `mcp-browser`, `mcp-dev-tools`, `vintage-vault` by hand), and the hook-directory glob copied a `.pyc` into 39 projects (removed everywhere). 25 tests pass. Only `regilo` remains dirty, with an uncommitted session's spec/queue/handoff work that is not migration-related.

- 2026-09-01 (end) — Migration complete. The user converted `regilo`; every template-derived project (40) is now at template version 1 and the tool reports nothing to update. Committed the migration in 37 projects with a script that staged only tool-written paths that were still exactly at the tool's target (message `chore: migrate to project-template-a layout v1`); `bishop-terminal` and `sampo` had been committed by their own sessions. Unrelated uncommitted work (bishop state files, TODO and initial-idea edits, a Makefile, stray Claude worktrees in `bishop-desktop`) was left alone.

- 2026-09-01 (final) — Tidied every carried-over section. No project had launcher customizations (all old `dev.sh` copies were plain template revisions), so no `dev.local.sh` was needed anywhere. Five projects had only template drift in the section and were trimmed mechanically; 18 with real custom lines got one Opus worker each (brief: fold project-specific `+` lines into "Project-specific rules", drop what `AGENTS.md` covers, delete the section, touch only `PROJECT.md`). All 18 reports were reviewed; every project's title was checked against HEAD after one worker reported a transient wrong-header read, and no cross-contamination was found. Committed `PROJECT.md` in 22 projects (`docs: fold carried-over instructions into PROJECT.md`); `Mezuril` was finished and committed by its own session, which also completed its first run. The migration work is complete.

- 2026-09-01 (evening) — Template v2: Claude Code can now dispatch Codex workers. Researched the Codex 0.152.0 CLI, smoke-tested `codex exec -C` in a linked worktree (no trust prompt, `AGENTS.md` loaded, `workspace-write` writes fine, the project's `.codex/hooks.json` rm-guard hook enforced), then added template-owned `dispatch-codex.sh` (worktree `.agent-worktrees/<slice>`, branch `codex/<slice>`, defaults Sol/high/workspace-write, artifacts under `.agent-worktrees/_runs/<slice>/`, `--remove` for cleanup), the dispatch paragraph in `AGENTS.md`, a mandatory worker preamble in `TASK_BRIEF_TEMPLATE.md`, the `.agent-worktrees/` ignore rule, README section, and `docs/spec/FEATURE_SPEC_codex_dispatch.md`. Bumped `TEMPLATE_VERSION` to 2, ran the migration (40 projects updated, tool reports all current), and committed it in each project (`chore: apply project-template-a v2 (Codex dispatch helper)`). The first real dispatch on a genuine slice is still to come; see the spec's deferred item.

### Next session

1. Nothing is pending for the migration. Try `dispatch-codex.sh` on a real slice in some project and judge the report quality; consider a structured `--output-schema` report if prose reports prove weak.
2. When a template-owned file changes in a way projects should receive, bump `TEMPLATE_VERSION`, add a `TEMPLATE_CHANGELOG.md` entry, and run `./migrate-project-docs.sh /mnt/Fast/projects`.
3. The superseded slice-9 stash and the template's `.migration-runs/` directory were dropped at the user's request; the old spec's incident record is the only remaining trace of those runs.

## Archives

- (none yet)
